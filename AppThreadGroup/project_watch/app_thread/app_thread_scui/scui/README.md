
# SCui(Gui框架, 整体构建完备):
    锚定目标:嵌入式运动手表
    锚定平台:4M/8M高速Psram + HW Acc
    实现目标:
        在廉价的平台上实现高效的显示效果
        结构化的解决方案
        体系化的项目构建
        模板化的界面开发

备注:
    2D框架最后节点-->030c1ec057de65c2968df1f383e59878ac77955f
    后续的内容是2.5D升级, 持续开发中...

备注:
    它需要从AppThreadGroup中使用一些额外的组件
    这部分组件甚至不做重新转接而是直接依赖使用
    原因是所依赖的组件具备较为完整的独立迁移性
    这意味着那么如果需要移植该整体结构模组时
    需要将所依赖的对应组件协同移植
    这部分内容具体见linker子目录


未完成计划:
    词条+图片:框架变现能力-让ai自行发掘可实现性
    ui presenter -- 与系统的桥梁
    多少可以对接到存储
    
    俩套分离，
    一套测试UI，用于扩充显示效果，
    一套正式UI，用于验证图形效果，
    让俩个都在一个里面
    
    文字:RTL和Bidi未实现,暂不支持小语种
    
    MP4支持度
    
    控件:支持lvgl式渲染(有限度)
        猜想:基础源控件还需完备
    控件:支持lvgl式symbol(可绘制, 但还不联动)
    控件:支持lvgl式布局(有限度)
        最有必要:控件的相对自动对齐(align有必要,其他好像???)
        猜想:自动对齐布局(draw_prepare—>layout)

问题点:
    app_sys_timer_reduce加点特殊指定信息，
    查一下app_thread_scui_timer工作异常问题
    刚才发现在app_sys_timer转换的node为NULL异常
    (基本都是scroll触发的)
    
    scroll的skip工作失效???



