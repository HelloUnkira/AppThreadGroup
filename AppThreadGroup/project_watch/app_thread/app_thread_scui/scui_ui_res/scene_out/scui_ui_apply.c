/*一个通过scui_widget_parser脚本生成的widget动态构造器配置
 */

#include "scui.h"

/*@brief 控件应用样式回调(make后统一应用样式)
 *@param handle 控件句柄
 */
void scui_ui_apply(scui_handle_t handle)
{
    /* UI 统一决定一套完整的样式风格交付给框架 apply */
    /* 目前为空框架, 后续按控件类型应用 prop/tran 样式 */
}
